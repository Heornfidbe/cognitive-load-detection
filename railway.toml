[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn run:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2"
healthcheckPath = "/"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
